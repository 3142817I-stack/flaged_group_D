# flaged_group_D
flag game 

## Prerequisites

- Python 3.11.x installed (the virtualenv should be created with the same minor
  version to avoid incompatibilities).

Then create the venv and run:

```powershell
python -m venv .venv
Activate.ps1
pip install -r requirements.txt